import { AppStrikethroughDirective } from './app-strikethrough.directive';

describe('AppStrikethroughDirective', () => {
  it('should create an instance', () => {
    const directive = new AppStrikethroughDirective();
    expect(directive).toBeTruthy();
  });
});
